# Discord Selfbot Configuration

# Your Discord user token (NOT a bot token)
# 
# METHOD 1 - Browser Local Storage (EASIEST):
# 1. Open Discord in browser (discord.com) and press F12
# 2. Go to Application tab (Chrome) or Storage tab (Firefox)
# 3. Expand "Local Storage" → https://discord.com
# 4. Find the "token" key and copy its value (remove quotes)
# 
# METHOD 2 - Console Commands (try these in order):
# 1. Press F12 → Console tab
# 2. Try one of these:
#    JSON.parse(localStorage.token)
#    window.webpackChunkdiscord_app.push([[Math.random()],{},e=>{Object.values(e.c).find(e=>e?.exports?.default?.getToken).exports.default.getToken()}])
# 
# METHOD 3 - Network Tab:
# 1. Press F12 → Network tab
# 2. Reload Discord (F5)
# 3. Find any request to discord.com/api
# 4. Look in Headers → Authorization (copy everything after "Bearer ")
DISCORD_TOKEN=your_discord_token_here

# Your BinX AI authentication token
# To get this:
# 1. Visit https://binx.cc and login
# 2. Open Developer Tools (F12) → Network tab
# 3. Go to AI Chat and send a message
# 4. Find the /api/ai-chat request
# 5. Copy the Authorization header value (without 'Bearer ')
# Format: USERID|TOKEN
# 
# Alternatively, you can create a binx_token.txt file in this directory
# with just your token (without BINX_TOKEN=)
BINX_TOKEN=your_binx_token_here
